import java.util.*;

class InsertionSort{
  public ArrayList<Double> sort(ArrayList<Double> userArr){
    for (int i = 0; i < userArr.size() - 1; i ++){
      int correctPos = i;
      int value = userArr.get(correctPos);
      for (int j = i; j > 0; j --){
        if(userArr.get(i) < userArr.get(j)){
          userArr.set();
        }
      }
    }
    return userArr;
    }
  }
